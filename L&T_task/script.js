// script.js
function register() {
    // Fetch user input values
    const name = document.getElementById('name').value;
    const email = document.getElementById('email').value;
    const password = document.getElementById('password').value;
  
    // TODO: Perform validation on the input fields
  
    // For demonstration purposes, log the data to the console
    console.log('Name:', name);
    console.log('Email:', email);
    console.log('Password:', password);
  
    // TODO: Send registration data to the server for further processing
    // Include logic on the server to send a verification email with an activation link
  }
  